# Application Constants

A centralized location for static data and configuration strings.

## Files
- **`appConstants.js`**: Contains route paths, reward thresholds, error messages, and UI configuration (like table columns and chart colors).
