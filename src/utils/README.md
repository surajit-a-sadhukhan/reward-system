# Utility Functions

Pure functions and helper classes for shared logic.

## Files
- **`rewardCalculator.js`**: Core business logic for calculating points from transaction amounts.
- **`dateUtils.js`**: Helpers for formatting dates and managing year-month strings.
- **`logger.js`**: A custom wrapper for `console` to provide structured, level-based logging.
