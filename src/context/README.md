# Application Context

State management using the React Context API.

## Files
- **`AuthContext.js`**: Manages the authentication state (user, token, login/logout functions) and persists it to `sessionStorage`.
