# Custom Hooks

Encapsulated logic for data fetching and state synchronization.

## Files
- **`useCustomers.js`**: Fetches and manages the list of all customers.
- **`useTransactions.js`**: Fetches and manages the transaction dataset.
